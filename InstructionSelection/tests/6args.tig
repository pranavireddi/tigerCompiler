let
    function sumSix(a: int, b: int, c: int, d: int, e: int, f: int) : int =
        a + b + c + d + e + f

    var result := 0
in
    result := sumSix(10, 20, 30, 40, 50, 60);
    print(chr(result)) 
end